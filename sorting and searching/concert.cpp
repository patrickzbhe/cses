#include <iostream>
#include <math.h>
#include <vector>
#include <string>
#include <bits/stdc++.h>
#include <algorithm>
#define lli long long int
#define li long int
#define ld long double
#include <fstream>
using namespace std;

bool vals[1200000000];

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ifstream inFile;
    int n,m;
    cin >> n >> m;
    multi
    return 0;
}